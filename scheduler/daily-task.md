# Daily scheduler task (live id: task-mt9kwxxt-hq9wqr)

Schedule: daily 13:15 America/Los_Angeles (4:15pm ET, after Simplify mail cutoff)

## Prompt

[Daily resume drops] Orchestrate the STAGED run — delegate every stage, do not run pipeline work inline (Felix 2026-08-26, effort lowered + staged 2026-08-29). MODEL SPLIT (Felix 2026-08-29, after an Opus session limit killed both appliers mid-run): Stage 1 'jd-list' and Stage 3 'jd-reconcile' run on Opus 5 because they carry the judgment — triage and coverage; Stage 2 'job-applier' runs on SONNET 5 because it is mechanical browser driving and is the bulk of the day's spend. All stages at medium effort. Stage definitions live in ~/zylos/.claude/skills/resume/SKILL.md "Daily pipeline" -> "Stage architecture"; agent files in ~/zylos/.claude/agents/ carry these pins in frontmatter. For any subagent_type below that is not registered in this session (registry loads at session start), fall back to subagent_type 'general-purpose' with effort 'medium' and the SAME per-stage model as above (jd-list/jd-reconcile -> 'opus', job-applier -> 'sonnet'), prepending: 'First read ~/zylos/.claude/agents/<name>.md and obey it as your system instructions.'

STAGE 1 — launch ONE background subagent 'jd-list': 'Run Stage 1 (LIST) of the daily pipeline for today per SKILL.md: STEP 0 FIRST — run `node ~/zylos/.claude/skills/resume/scripts/pipeline-check.js sync local` (cross-machine seen-sync via resume-drops/state/, SKILL.md Stage 1 step 0) and report its {before, after, peers, published} line, then proceed: SOURCE = the daily SWElist email at felixl0808@gmail.com (SimplifyJobs-lists sweep only if no new email), fetch every JD's facts with swelist-fetch.py + jd-fetch.js (no per-row curl), facts-only triage over the JSON (bachelors-level, US-located, exclusions incl. TikTok/ByteDance), write joblist.json, return counts + path + selected keys in email order.' Wait for its result.

STAGE 1.5 — TAILOR (you run it inline, it is a script): if M > 25, pick wave one = the 25 most promising keys first. Run `node ~/zylos/.claude/skills/resume/scripts/tailor-batch.js --joblist <joblist path> [--keys <wave keys>]` (default lanes = min(4, cpus-1)); it writes every PDF into resume-drops/<date>/ and runs/<date>/tailor.json. Keep its summary (tailored / skipped-repost / failed counts, JD skills that did not fit, study list) for the DM. If it reports failures, re-run once with --keys <failed keys>; still-failed keys go to Stage 2 as parked-for-Felix, not skipped.

STAGE 2 — if 0 rows selected, skip to Stage 3. Else: start the shared browser once (~/zylos/.claude/skills/resume/scripts/pipeline-browser.sh start). Split the wave's keys into N = min(2, ceil(M/4)) contiguous slices (HARD CAP 2 — proven on 2026-09-02 that 3 parallel appliers on this Mac crash Chrome under memory pressure; the remainder of a >25 day runs as a second wave — Stage 1.5 for those keys, then appliers — after wave one returns). COVERAGE ASSERTION before launch: the union of the slices must equal the wave's key set exactly — every wave key in exactly one slice, no key in two, none dropped. Print the per-slice key counts and confirm they sum to M before spawning (on 2026-09-02 one selected row, Microsoft Data Science, was assigned to no slice and only Stage 3's coverage check caught it). Launch the N 'job-applier' subagents IN ONE message so they run in parallel; each prompt carries the joblist path, the tailor.json path, its part index i, and its keys: 'Run Stage 2 (APPLY) per SKILL.md for exactly these keys; PDFs are already in tailor.json — never tailor yourself; ats-fill.js first, snapshot -i not screenshots, answers from application-profile.json + answer-bank.md, no JD re-verification, no confirmation-email waits, compact ledger blocks; obey the concurrency rules (AGENT_BROWSER_SESSION=applier<i> on every agent-browser call with your own tab, write only ledger-part<i>.md, never push/stop the display).' Wait for ALL to return.

STAGE 3 — launch ONE 'jd-reconcile' subagent: 'Run Stage 3 (RECONCILE) per SKILL.md: merge ledger parts -> ledger.md, inbox verification, COVERAGE check (every joblist row accounted for: dropped/skipped/submitted/failed/parked — process any miss yourself inline), lean day README with ONLY Applied (successful company + role) and Manual (selected failed/parked jobs Felix must finish: company + role + apply link + PDF + one short blocker), update resume-drops/PROGRESS.md (append or update today's row, recompute the totals block — grand-scheme tracker, Felix 2026-09-02), push resume-drops + apply, prune, one summary DM to Felix on the Discord DM endpoint in RESUME_DM_ENDPOINT from ~/zylos/.env (per-host, never hardcoded) that ENDS with the Grand Scheme block from PROGRESS.md (cumulative submitted, manual outstanding, queue remaining), run `node ~/zylos/.claude/skills/resume/scripts/pipeline-check.js sync local` after the day's marks are all in (publishes today's keys for the droplet — SKILL.md Stage 1 step 0), stop the browser via pipeline-browser.sh stop (verifies CDP 9222 is dead).' Wait for its result.

While stages run, stay responsive to other messages; do NOT duplicate stage work inline. Sanity-check Stage 3's summary (README pushed? DM sent? coverage clean? display stopped?) and only then mark this occurrence done. If a stage's subagent dies, relaunch that stage once (appliers: relaunch only the dead instance with its keys); if the staged path fails twice overall, run the whole day via ONE 'resume-pipeline' fallback subagent (solo monolith) and tell Felix the parallel path failed. If Chrome is left running after a failure, run pipeline-browser.sh stop yourself.
