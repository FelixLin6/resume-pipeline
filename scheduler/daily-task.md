# Daily scheduler task (live id: task-mt9kwxxt-hq9wqr)

Schedule: daily 13:15 America/Los_Angeles (4:15pm ET, after Simplify mail cutoff)

[Daily resume drops] Orchestrate the STAGED run — delegate every stage, do not run pipeline work inline (Felix 2026-08-26, effort lowered + staged 2026-08-29: all stages execute on Opus 5 medium effort to save Fable credits). Stage definitions live in ~/zylos/.claude/skills/resume/SKILL.md "Daily pipeline" -> "Stage architecture"; agent files in ~/zylos/.claude/agents/. For any subagent_type below that is not registered in this session (registry loads at session start), fall back to subagent_type 'general-purpose' with model 'opus' and prepend: 'First read ~/zylos/.claude/agents/<name>.md and obey it as your system instructions.'

STAGE 1 — launch ONE background subagent 'jd-list': 'Run Stage 1 (LIST) of the daily pipeline for today per SKILL.md: SOURCE = the daily SWElist email at felixl0808@gmail.com (SimplifyJobs-lists sweep only if no new email), visit every JD through its own link, facts-only triage (bachelors-level, US-located, exclusions incl. TikTok/ByteDance), write joblist.json, return counts + path + selected keys in email order.' Wait for its result.

STAGE 2 — if 0 rows selected, skip to Stage 3. Else: start the shared browser once (~/zylos/bin/zylos-browser display start). Split the selected keys into N = min(3, ceil(M/4)) contiguous slices (if M > 25, wave one = the 25 most promising, remainder as a second wave after wave one returns). Launch the N 'job-applier' subagents IN ONE message so they run in parallel; each prompt carries the joblist path, its part index i, and its keys: 'Run Stage 2 (APPLY) per SKILL.md for exactly these keys; obey the concurrency rules (tailor under flock, own browser tab, write only ledger-part<i>.md, never push/stop the display).' Wait for ALL to return.

STAGE 3 — launch ONE 'jd-reconcile' subagent: 'Run Stage 3 (RECONCILE) per SKILL.md: merge ledger parts -> ledger.md, inbox verification, COVERAGE check (every joblist row accounted for: dropped/skipped/submitted/failed/parked — process any miss yourself inline), minimal day README (ONLY postings left for Felix: apply link + PDF), push resume-drops + apply, prune, one summary DM to Felix on Discord DM 1511039204791156786, stop the browser display.' Wait for its result.

While stages run, stay responsive to other messages; do NOT duplicate stage work inline. Sanity-check Stage 3's summary (README pushed? DM sent? coverage clean? display stopped?) and only then mark this occurrence done. If a stage's subagent dies, relaunch that stage once (appliers: relaunch only the dead instance with its keys); if the staged path fails twice overall, run the whole day via ONE 'resume-pipeline' fallback subagent (solo monolith) and tell Felix the parallel path failed. If Chrome is left running after a failure, stop the display yourself.
